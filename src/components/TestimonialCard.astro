---
interface Props {
  name: string;
  result: string;
  quote: string;
}

const { name, result, quote } = Astro.props;
---

<div class="bg-white rounded-2xl shadow-md p-8 hover:-translate-y-1 transition-transform duration-300">
  <!-- Stars -->
  <div class="flex gap-1 mb-4" aria-label="5 stars">
    {[1, 2, 3, 4, 5].map(() => (
      <svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24" fill="#D4A5A0" stroke="none">
        <polygon points="12 2 15.09 8.26 22 9.27 17 14.14 18.18 21.02 12 17.77 5.82 21.02 7 14.14 2 9.27 8.91 8.26 12 2"/>
      </svg>
    ))}
  </div>

  <!-- Quote -->
  <blockquote class="text-brown-dark/80 leading-relaxed mb-6 italic text-base">
    "{quote}"
  </blockquote>

  <!-- Attribution -->
  <div>
    <p class="font-semibold text-brown-dark text-sm">{name}</p>
    <p class="text-blush-dark text-xs mt-0.5">{result}</p>
  </div>
</div>
